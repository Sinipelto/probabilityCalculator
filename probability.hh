#ifndef PROBABILITY_HH
#define PROBABILITY_HH

#include <thread>
#include <chrono>
#include <random>
#include <iostream>

bool probability(unsigned const n, bool const debugMode);

#endif // PROBABILITY_HH
